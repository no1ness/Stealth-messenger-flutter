import type { ApiMediaFormat } from '../../types';

export async function downloadMedia(_fileId: string, _mediaFormat: ApiMediaFormat): Promise<Blob | Uint8Array | string | undefined> {
  return undefined;
}
