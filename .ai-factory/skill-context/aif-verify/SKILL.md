# Project Rules for /aif-verify

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-05-23
> Based on: 2 analyzed patches + `.ai-factory/rules/base.md`

## Rules

### Flutter SDK availability gating
**Source**: patch 2026-05-12 ("If Flutter SDK is unavailable in PATH, pubspec.lock cannot be regenerated...") + `.ai-factory/rules/base.md` ("Если Flutter/Dart недоступны в PATH, явно сообщить, что проверка не выполнена")
**Rule**: Before asserting `Build: passes` / `Tests: pass` / `Lint: clean` in the verification report, confirm that `flutter` is actually on PATH and that `flutter analyze`, `flutter pub get`, and `flutter test` ran to completion (exit code 0). If any of them fails with "command not found" or doesn't run at all, the verification report MUST surface this gap explicitly as a WARN row — not silent green — with the exact commands the owner needs to run manually: `cd client && flutter pub get && flutter analyze && flutter test`.

### no_bare_logging regression gate
**Source**: tech-fact, `client/test/security/no_bare_logging_test.dart` + `.ai-factory/rules/base.md`
**Rule**: This project enforces a regression gate on bare `debugPrint(`/`print(` calls in `client/lib/` — every PR must keep that test green. The allow-list inside the test is intentionally hand-maintained and is supposed to shrink over time. During verification, confirm: (a) the test passed in the run, (b) the allow-list did not grow (compare against the `target: 0` comment line; if it grew, that's a regression even if the test technically passes), (c) any new code under `client/lib/` uses `Logger.*` exclusively.
