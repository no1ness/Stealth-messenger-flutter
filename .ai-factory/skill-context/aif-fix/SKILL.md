# Project Rules for /aif-fix

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-05-23
> Based on: 2 analyzed patches

## Rules

### Test-stability fixes: check widget ctor for env coupling
**Source**: patch 2026-04-30 ("WebRTC Call Screen Test Stability")
**Rule**: When fixing a widget-test regression that surfaces after env/transport changes, first check whether the widget constructor or `initState()` depends on `dotenv.env` / `flutter_dotenv` load state. Move env-dependent wiring out of the widget and into `main.dart` / `LocalAppService` so the constructor stays test-safe. Do NOT "fix" the test by calling `dotenv.load()` from setUp — that just masks the underlying coupling.

### Explicit Flutter SDK PATH gap
**Source**: patch 2026-05-12 ("If Flutter SDK is unavailable in PATH...") + `.ai-factory/rules/base.md`
**Rule**: If `flutter` is not on PATH, never silently claim verification passed. Run `flutter analyze`/`pub get`/`test` and check exit codes; on failure with "command not found" or equivalent, surface explicitly: "Verification incomplete — Flutter SDK unavailable in PATH. Recheck with `cd client && flutter pub get && flutter analyze && flutter test`." Include this gap in the fix summary so the owner knows what wasn't verified.
