# Project Rules for /aif-improve

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-05-23
> Based on: 5 `/aif-improve` refinement passes on `feature/diagnostics-logs-screen` (concrete evidence of recurring hallucinations)

## Rules

### Verify against source, not previous plan text
**Source**: 5 refinement passes on `feature/diagnostics-logs-screen` each caught a different hallucinated API in the prior plan (Pass 1: `Logger.redact()` did not exist; Pass 2: `GroupSecretService.knownGroupIds()` did not exist, `bucketReady` was a hardcoded constant masquerading as a health signal; Pass 3: `GlassPageRoute` did not exist, `WebRtcSignalingService` was per-call not singleton; Pass 4: concrete service ctors had no DI seams; Pass 5: `LocalAppService` lifecycle leak through final-field ownership).
**Rule**: When refining a plan, every named API (method, getter, widget class, field) referenced in a task description MUST be verified against the actual source under `client/lib/` and `client/test/` — via `grep`/`Read` — before being kept in the refined plan. Plan-text from a previous pass is NOT the source of truth; code is. Pay special extra attention to:
- Method signatures (this project has `Logger.{level}(message, {extras})` with no `tag` arg)
- Widget class existence (Apple Liquid does NOT have `GlassPageRoute`)
- Service lifecycle (per-call vs singleton vs per-widget-state)
- Return shapes — some Map<String, dynamic> fields are hardcoded constants, not real signals (`DashboardService.bucketReady`, `AttachmentService.bucketReady`)
- DI seams — services with parameterless constructors that create their own deps cannot be faked; aggregator ctors need provider-closure parameters
