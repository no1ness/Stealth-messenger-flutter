# Evolution: 2026-05-23 00:00

## Intelligence Summary

- **Patches analyzed:** 2
  - `2026-04-30-13.29.md` — WebRTC call screen test stability
  - `2026-05-12-local-only-supabase-removal.md` — Remove legacy cloud backend
- **Additional sources:**
  - 5 `/aif-improve` refinement passes on `feature/diagnostics-logs-screen` (concrete evidence of hallucinated APIs being caught and fixed)
  - `.ai-factory/rules/base.md` (cross-check for duplicates — already-covered items dropped)
  - `.github/workflows/ci.yml` (Supabase guard)
- **Tech stack:** Dart / Flutter (3.x SDK), `cryptography`, `flutter_webrtc`, `pocketbase`, `idb_shim` / `sqflite`, `flutter_dotenv`, Apple Liquid design system in `client/lib/themes/apple_liquid/`
- **Cursor:** advanced to `2026-05-12-local-only-supabase-removal.md` (newest patch)

## Improvements Applied

### aif-plan → skill-context (5 rules)
**File:** `.ai-factory/skill-context/aif-plan/SKILL.md`

- Verify Apple Liquid widget existence before referencing (`GlassPageRoute` does not exist) ← derived from `/aif-improve` Pass 3
- Verify Logger signature (no `tag` arg) ← derived from `/aif-improve` Pass 1
- Map service lifecycles before integrating (`WebRtcSignalingService` per-call, `LocalAppService` per widget-state) ← patch 2026-04-30 + `/aif-improve` Pass 3/5
- Provider-closure DI for testable aggregators ← `/aif-improve` Pass 4
- Reject new cloud-backend proposals ← patch 2026-05-12 + `rules/base.md`

### aif-implement → skill-context (7 rules)
**File:** `.ai-factory/skill-context/aif-implement/SKILL.md`

- Verify service API before generating code (hardcoded `bucketReady`) ← `/aif-improve` Pass 2
- `dotenv` direct read for graceful degradation (`PocketBaseClient.instance` throws on empty env) ← `/aif-improve` Pass 2
- Test-safe widget constructors ← patch 2026-04-30
- `share_plus` on web path (no `path_provider`) ← tech-fact from implementation work
- Inline scrubber for any exported log text ← `/aif-improve` Pass 1 (security finding)
- Update `.ai-factory/` context with arch/stack changes ← patch 2026-05-12
- CI Supabase guard awareness ← `ci.yml:45`

### aif-fix → skill-context (2 rules)
**File:** `.ai-factory/skill-context/aif-fix/SKILL.md`

- Test-stability fixes: check widget ctor for env coupling ← patch 2026-04-30
- Explicit Flutter SDK PATH gap ← patch 2026-05-12 + `rules/base.md`

### aif-improve → skill-context (1 rule)
**File:** `.ai-factory/skill-context/aif-improve/SKILL.md`

- Verify against source, not previous plan text ← 5-pass refinement evidence on the diagnostics feature (each pass caught a different hallucinated API)

### aif-verify → skill-context (2 rules)
**File:** `.ai-factory/skill-context/aif-verify/SKILL.md`

- Flutter SDK availability gating ← patch 2026-05-12 + `rules/base.md`
- `no_bare_logging` regression gate ← `client/test/security/no_bare_logging_test.dart` + `rules/base.md` (added beyond the 1-rule proposal because the same source supported a second, complementary rule)

## Patterns Identified

- **Hallucinated API references in plans** — 5 occurrences in a single feature; primary mitigation in `aif-plan` + `aif-improve` rules requiring source-grep before naming any API.
- **Hardcoded values masquerading as health signals** — 2 instances (`DashboardService.bucketReady`, `AttachmentService.bucketReady`); covered by `aif-implement` rule "Verify service API before generating code".
- **Per-call vs singleton lifecycle confusion** — 1 critical instance (`WebRtcSignalingService`); covered by `aif-plan` rule "Map service lifecycles".
- **Test-safety: env coupling in widget ctors** — 1 patch + reinforced by `share_plus` work needing `kIsWeb` branching; covered by `aif-implement` and `aif-fix`.
- **Local-first commitment drift risk** — covered both at planning (reject proposals) and verification (CI Supabase guard) layers.

## Skipped (Already in `rules/base.md`)

Not duplicated in skill-context:
- `Logger.*` usage as the only logging path
- `mounted` check after `await` before `setState`
- `snake_case.dart` / `PascalCase` / `camelCase` naming
- `package:stealth/...` import convention
- Local-first project rule (mentioned in skill-context only where a planning-time decision is needed)
